apiVersion: apps/v1
kind: Deployment
metadata:
  name: nebius-sample-gpu-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nebius-sample-gpu-app
  template:
    metadata:
      labels:
        app: nebius-sample-gpu-app
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodepool
                    operator: In
                    values:
                      - nebius-gpu-nodepool
      containers:
        - name: gpu-container
          image: nvidia/cuda:12.4.0-base-ubuntu22.04
          command: ["nvidia-smi", "-l", "60"]
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
              nvidia.com/gpu: "1"
            limits:
              memory: "1Gi"
              cpu: "500m"
              nvidia.com/gpu: "1"